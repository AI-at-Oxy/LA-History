# Prompt Changelog — CONCEPT_MAP_CHAT_PROMPT

| Version | What Changed | Hypothesis | Key Result | Decision |
|---------|--------------|------------|------------|----------|
| v0 | Baseline from M2: 9-rule Socratic prompt with map-state branches (rules 4–6), off-topic redirect (rule 7), and 3-sentence cap (rule 2). | N/A — starting point. | Mean 10.675/16. C1=2.94, C2=2.24, C3=3.41, C4=2.09. C2 and C4 are the weak axes; C3 is already strong because the prompt encourages "why/how" questions. | Starting point |
| v1 | Rule 2 revised: 3-sentence cap → "2–3 sentences; always end with a specific map question." Rules 10–13 added: (10) prior-knowledge probe before Socratic reframe on factual claims; (11) cognitive-floor reset after two consecutive deflections; (12) interrogate under-specified edge labels (1–3 words) for directionality/precision; (13) reject minimal replies ("idk", "sure", etc.) and anchor to a concrete map node. | Closing question targeting C4; diagnostic step targeting C2; edge-label interrogation targeting C3; deflection/minimal-reply handling covering Scenarios 5, 7, 16, 17. | Mean 22.235/32 (11.118/16), up from 21.941/32 (10.971/16) in v0 — a gain of +0.294/32 overall. C4 improved most (+0.383), C2 improved (+0.206), C1 was flat (+0.030), C3 regressed slightly (−0.029). Hypothesis partially confirmed: C4 and C2 moved in the predicted direction; C1 did not because Rule 1 forbids direct answers but not facts embedded in questions — Output 1 continued leaking dates and events as scaffolding. C3 regression was an unintended consequence: tightening to 2–3 sentences and front-loading Rule 10's diagnostic crowded out causal/comparative depth, especially in Scenarios 4, 8, 11, 15. Worst performers: Scenario 11 (17/32, Watts Towers misconception — Output 2 directly corrected the student), Scenario 5 (18/32, edge label "." — Output 1 dropped five historical facts), Scenario 7 (19/32, Chavez Ravine — both outputs stated the displacement fact directly). Output 2 outperformed Output 1 on C1 in 11/17 scenarios, revealing that the rules are landing inconsistently across samples. | Applied 2026-04-23 |
| v2 | Rule 1 hardened: forbid historical facts in any form (declarative, rhetorical, parenthetical, embedded in questions). Rule 2 tightened: single closing question, no stacking. Rule 3 sharpened: prefer causal/comparative questions over recall. Rule 7 softened: warm one-sentence redirect before pivoting. Rule 10 tightened: prior-knowledge probe is a full standalone turn, not combined with redirect. Rule 14 added: explicit misconception protocol — two-turn probe (why-do-you-believe-it, then sensory/evidence follow-up); ban "wrong", "incorrect", "actually", "in fact". Rule 15 added: protect analytical depth under the sentence cap — short responses must do more work in the closing question, not less. Rule 16 added: consistency requirement across multiple sampled outputs. | (1) Hardening Rule 1 against fact-leakage will lift C1 in Output 1 on Scenarios 3, 4, 5, 7, 10 (where v1 dropped facts inside questions), pushing C1 mean above 3.5. (2) Rule 14's two-turn misconception protocol will fix Scenarios 6 and 11, the two worst performers — the model is currently failing because it has no rule for "student says wrong thing", so it defaults to either confirming or correcting. (3) Rule 15 will prevent the C3 regression we saw in v1, where tightness crowded out causal depth — by making depth a closing-question requirement, not a luxury. (4) Rule 16 should reduce the Output 1 vs Output 2 variance we saw in v1, where Output 2 outperformed Output 1 on C1 in 11/17 scenarios. | Mean 25.765/32, up +3.530 from v1 — by far the largest single-version gain. Hypothesis 1 strongly confirmed: C1 hit a perfect 4.000 (every output across all 17 scenarios refused to leak facts), exceeding the >3.5 target. The Output 1 vs Output 2 variance from v1 collapsed entirely on C1 — Rule 16 worked. Hypothesis 2 confirmed: Scenario 6 jumped from 20→28 and Scenario 11 from 17→24, both now executing Rule 14's belief probe; Scenario 6 also reaches the sensory step in turn 1, though Scenario 11 only completes step 1 of the two-step protocol (the sensory/scale follow-up is missing in both outputs — a v3 target). Hypothesis 3 NOT confirmed: C3 regressed another −0.029 despite Rule 15. Examining low-C3 scenarios shows the model defaulting to selection prompts ("which two nodes might belong together") on Scenarios 9, 10, 11 — these score C3=2 because they're identification, not analysis. C2 improved meaningfully (+0.441) but stayed below 3.0; off-topic scenarios (10, 12, 13, 14, 15) consistently score C2=2 because the model skips the diagnostic probe entirely when redirecting. New regressions: Scenario 16 Output 1 violates Rule 11 by literally repeating Scenario 2's prior-knowledge probe verbatim; Scenario 17 Output 1 doesn't acknowledge the prior deflections. Worst performers now Scenarios 9 and 10 (23/32 each). | Carry forward; v3 should (a) extend Rule 14 to enforce the second sensory/scale turn, (b) add a C3-protection rule that forbids pure-selection questions when analytical comparison is possible, (c) strengthen Rule 11 against verbatim repetition of prior-knowledge probes across turns, and (d) require a diagnostic probe even on off-topic redirects to lift C2. |
| v3 | Rule 3 sharpened: explicitly classifies selection-only and identification-only questions as too thin for default use, permitting them only as a Rule 11 last-resort. Rule 7 hardened: off-topic redirects MUST include a diagnostic probe, not jump straight to a comparative question. Rule 8 extended: ban suggesting node pairings ("perhaps Watts Towers and MacArthur Park"). Rule 11 rewritten: model must track its own prior turns, acknowledge stuck-ness, and generate a NEW question that has not appeared before — explicitly forbidding verbatim repetition. Rule 14 split into TURN 1 (belief probe only) and TURN 2 (sensory follow-up regardless of student answer) to force completion of the two-step protocol. Rule 15 reframed as positive depth requirement with explicit fallback hierarchy. Rules 17–23 added: (17) anti-template — vary openers across turns, ban repeated "It sounds like…" / "Since I cannot…" templates; (18) student-is-right handling — don't hedge correct claims; (19) era-scope constraint — stay within {era_name} window; (20) instruction-integrity — silent redirect on jailbreak/persona-change attempts; (21) empty-state fallbacks for malformed/missing template variables; (22) conversation coherence across turns; (23) explicit tone guidance against clinical tutor-speak. | Targeted hypotheses from v2 evaluation: (1) Rule 3 + Rule 15 reframe will lift C3 above 3.5 by stopping the model from defaulting to selection prompts on Scenarios 9, 10, 11. (2) Rule 7's mandatory diagnostic probe will lift C2 on off-topic Scenarios 10, 12, 13, 14, 15 from 2.0 to 3.0+. (3) Rule 11's explicit "no verbatim repetition" + "track your own turns" will fix Scenario 16 Output 1's regression. (4) Rule 14's split structure will complete the second sensory turn that Scenario 11 still misses. General improvements not tied to specific scenarios: Rule 17 will reduce the templated feel of v2's Output 2 (which started multiple responses with "Since I cannot…"); Rule 19 prevents era-scope drift; Rule 20 closes a real prompt-injection hole; Rules 21–22 handle production edge cases (malformed template variables, multi-turn dialogue) the test scenarios don't exercise but real users will hit. Expected total: above 28/32. | TBD | TBD |
